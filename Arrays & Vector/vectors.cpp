#include<bits//stdc++.h>
using namespace std;

int main(){

    //syntax
    vector<int>a;
    a.push_back(1);
    a.emplace_back(2);

    //pairs in vecter
    vector<pair<int,int>>b;

    b.puch_back({1,2});
    b.emplace_back(3.4);

    //vector with already defined size

    vector<int>c(5,100);
    vector<int>d(5);

    //copy
    vector<int>v1(5,20);
    vector<int>v2(v1);

    //access vector elements

    cout<<v1[0]
    

}